<h1>Menu</h1>

<% if flash[:notice] %>
  <p><%= flash[:notice] %></p>
<% end %>

<h4>Your Cart: </h4>

🤔
<ol>
  <% if @cart_nachos %>
    <% @cart_nachos.each do |nacho| %>
    <li><%= nacho.name %></li>
    <% end %>
  <% end %>
</ol>


<hr>

<% @nachos.map do |nacho| %>
  <ul>
    <li>
      <%= link_to nacho.name, nacho %>
      $<%= nacho.price %>
      <%= button_to "Add to cart :)", "/cart", {method: :patch, params: {nacho_id: nacho.id}} %>
    </li>
  </ul>
<% end %>
